# Analysis scripts

Analysis and audit scripts are currently kept at `scripts/*.py` because several of them import each other through `scripts.*`.
See `scripts/README.md` for the logical inventory and recommended commands.
