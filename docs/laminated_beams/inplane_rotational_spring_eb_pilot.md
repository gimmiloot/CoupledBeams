# Первый EB-пилот поворотно-упругого узла

Дата подготовки и вычислительного продолжения: **2026-09-10**. Checkout:
`D:/PHD/CoupledBeams/CoupledBeams`, ветка `main`, исходный HEAD
`a960fefab7309b38ef1ae93b1208a9859ac8af65` (`Version 0.5.1`), исходное дерево чистое.
Это историческая версия подготовки кода. Вычислительное продолжение начато на
HEAD `acb3078c497b9d3366b4d1ecb1f12b3e6f50db43` (`Version 0.5.1.1`), также с чистым
деревом. Доработки runner, тестов и отчёта — **working-tree version** к этому HEAD;
физический EB-модуль не изменялся.

**Состояние: EB-реализация исполняется; 50 целевых тестов пройдены.
Приняты 10/12 BASE-групп, сохранены 70/84 корней. Пилот ЧАСТИЧНЫЙ.**
Обе группы `kappa=10000` не прошли физическую невязку пружины; дополнительная
legacy regression для RIGID при 30° не разрешила два обнаружения guard 7.
Эти случаи не объявлены успешными. Первоначальный `NOT_RUN_ENVIRONMENT`
снят после обнаружения существующего окружения; общий PASS модели не присвоен.

## Постановка и реализация

Основание — [теоретическая заметка](inplane_rotational_spring_joint.md), разделы
«Шесть условий и матрица узла», «Независимый вывод EB», «Пределы» и
«Характеристическая постановка». Сохранён
[физический координатный контракт](reddy_inplane_coordinate_contract.md): обе
оси идут от внешних заделок к узлу, `y=[u,w,psi,N,Q,M]`, угол публичного API в радианах.
Поступательная связь остаётся жёсткой; точечная безмассовая ненапряжённая пружина
действует только на `Delta_psi=psi1-psi2`. Вязкость, эксцентриситет и масса узла отсутствуют.

В [inplane_rotational_spring_eb.py](../../scripts/lib/inplane_rotational_spring_eb.py)
подготовлены `EBArm`, `Joint`, `state_matrix`, `transfer_matrix`,
`clamp_to_joint_map`, `joint_matrix`, `boundary_assembly`, `endpoint_diagnostics`.
`A,D,m,L` должны быть конечными и положительными; `A` — продольная жёсткость,
`D` — изгибная жёсткость. `SPRING` требует явного конечного `k_theta>=0`;
`RIGID` требует `k_theta=None` и вызывает прежнюю матрицу жёсткого узла.
Нулевая пружина, отсутствие параметра и точный `RIGID` различаются.

Уравнения состояния классической EB:

```text
u'=N/A; w'=-psi; psi'=M/D;
N'=-m*omega^2*u; Q'=-m*omega^2*w; M'=Q.
```

При `c=cos(beta)`, `s=sin(beta)` шесть строк на концах `x_i=L_i`:

```text
u1+c*u2+s*w2=0
w1-s*u2+c*w2=0
M1+k_theta*(psi1-psi2)=0
N1-c*N2-s*Q2=0
Q1+s*N2-c*Q2=0
M1+M2=0.
```

Пять строк сохранены из
[reddy_symmetric_coupled_beams.py](../../scripts/lib/reddy_symmetric_coupled_beams.py),
`joint_matrix`; для `RIGID` третья строка остаётся `psi1-psi2=0`.
Пружинная строка имеет ненулевые элементы `(3,6,9)=(k_theta,1,-k_theta)`
при нумерации столбцов с 1. Минор по столбцам `(1,2,4,5,6,12)` равен 1.
Теоретическое тождество работы сохраняется: `delta W_arms=-delta U_J`,
`delta W_arms+delta U_J=0`. Прежняя атрибуция ошибочного минуса заданию снята
в теоретической заметке, RLB-K05, текущем контексте и CHANGELOG; формулы не менялись.

Новый модуль изолирован от старых входных функций. Старые defaults, state/joint
матрицы и результаты не редактировались. Импорт общих геометрических и численных
функций не означает реализацию нового узла Reddy/FSDT.

## Масштабирование и заранее заданные критерии

Для каждого плеча используется `Z_i=diag(L_i,L_i,1,A_i,D_i/L_i^2,D_i/L_i)`:
`T_i=Z_i exp(Z_i^-1 H_i Z_i L_i) Z_i^-1`, `P_i=T_i[:,3:6]` (индексы Python).
Физическая граничная матрица `B=J_theta blockdiag(P1,P2)` имеет размер 6×6.
Численные неизвестные — реакции заделок, делённые на `Z_i[3:6]`.

Единицы строк: `(l,l,D_ref/l,D_ref/l^2,D_ref/l^2,D_ref/l)` для пружины;
у `RIGID` единица третьей строки равна 1. После этого пружинная строка
делится на `max(1,kappa_theta)`, где `kappa_theta=k_theta*l/D_ref`.
Размерный `k_theta` не складывается с числом 1. Нулевая жёсткость не является
делителем. Далее применяется существующая `positively_equilibrate_matrix`.
Все преобразования обратимы; положительные множители строк умножают
определитель на ненулевой множитель и аналитически сохраняют его нули.

Для восстановления применяется обратное преобразование столбцов SVD, затем
масштаб реакций; сохраняются концевые состояния всех обнаруженных нуль-векторов.
Амплитуда нормируется по единицам состояния `(l,l,1,D_ref/l^2,D_ref/l^2,D_ref/l)`.
Шесть физических остатков вычисляются отдельно скалярными формулами и делятся
на фиксированные единицы строк, **без** делителя `max(1,kappa_theta)`.
Так большая жёсткость не скрывает ошибку закона пружины малым `Delta_psi`.
Концевые состояния служат только диагностикой; формы и модальная классификация не строятся.

Допуски записаны до получения результатов:

| Проверка | Критерий |
| --- | --- |
| `sigma_min/sigma_max` полной масштабированной матрицы | ≤ `1e-9` |
| Строгая численная nullity | `sigma_j/sigma_max ≤ 1e-12` |
| Невязка нуль-вектора относительно `||B||_F ||v||` | ≤ `1e-9` в обоих численных представлениях |
| Поступательная совместность в физических единицах строк | ≤ `1e-10` |
| Все физические строки; оба момента при шарнире | ≤ `1e-9` |
| Относительная частотная разность контрольных постановок | ≤ `1e-9` |
| Допустимое отрицательное приращение sorted частоты | не ниже `-1e-9` относительно предыдущей |
| Алгебра матриц/энергии в целевых тестах | `1e-12` |

Критерии сингулярности, nullity, совместности, равновесия и частотного сравнения
взяты из `THRESHOLDS` существующего
[матричного пилота](../../scripts/analysis/laminated_beams/pilot_reddy_symmetric_coupled_beams_beta0.py).
Новый критерий монотонности использует тот же относительный допуск частот:
положительное приращение энергии пружины при неизменной массе даёт неубывание
упорядоченных собственных частот по минимаксному принципу. Строгий рост не требуется.
Допуски принятия корней и физических невязок не менялись по результатам.
Два превышения сохранены как отказы, а не устранены ослаблением критерия.

## Конечный набор и поиск

Reference, а не прототип устройства: `L1=L2=l=1`, `b=.20`, `h=.05`, `E=rho=1`;
`Ag=b*h`, `Ig=b*h^3/12`, `A=E*Ag`, `D=D_ref=E*Ig`, `m=rho*Ag`.
`Omega=omega*l^2*sqrt(rho*Ag/(E*Ig))`, `Lambda=sqrt(Omega)`.
Для конечной пружины `k_theta=kappa_theta*D_ref/l`; RIGID сохраняется строковой меткой.

| beta, град | kappa_theta / режим | Omega, позиции 1–6 | guard 7 |
| --- | --- | --- | --- |
| 0 | 0 | 3.51601527; 15.41820572; 22.03449156; 49.96486203; 61.69721441; 104.24769646 | 108.82796185 |
| 0 | 0.1 | 3.71049804; 15.41820572; 22.43030331; 49.96486203; 62.08602526; 104.24769646 | 108.82796185 |
| 0 | 1 | 4.56324631; 15.41820572; 24.81998394; 49.96486203; 64.81705480; 104.24769646 | 108.82796185 |
| 0 | 100 | 5.57286351; 15.41820572; 30.07558365; 49.96486203; 74.27513642; 104.24769646 | 108.82796185 |
| 0 | 10000 | НЕ ПРИНЯТА: физическая невязка | — |
| 0 | RIGID | 5.59332136; 15.41820572; 30.22584793; 49.96486203; 74.63888382; 104.24769646 | 108.82796185 |
| 30 | 0 | 14.75413871; 15.41478605; 41.09736579; 49.91917565; 70.28567341; 102.91681248 | 109.81555611 |
| 30 | 0.1 | 14.91196749; 15.41478605; 41.10161574; 49.91917565; 70.48483772; 102.91681248 | 109.81555611 |
| 30 | 1 | 15.41478605; 15.96459803; 41.13109008; 49.91917565; 71.88941782; 102.91681248 | 109.81555611 |
| 30 | 100 | 15.41478605; 19.33201419; 41.23936368; 49.91917565; 77.17657808; 102.91681248 | 109.81555611 |
| 30 | 10000 | НЕ ПРИНЯТА: физическая невязка | — |
| 30 | RIGID | 15.41478605; 19.46501361; 41.24411469; 49.91917565; 77.40365971; 102.91681248 | 109.81555611 (legacy control не разрешён) |

[Entry point](../../scripts/analysis/laminated_beams/pilot_inplane_rotational_spring_eb.py)
задаёт локальный `frequency-map-v1/fast_plot`, `sorted_positions` по
[общей политике](../numerics/frequency_map_computation_policy.md). Это отдельный
диагностический сценарий новой физики и нового контракта выходных данных,
а не пресет материала прежнего RLB-runner; общие solver defaults не меняются.
Переиспользованы `_scan_candidates` и его локальный refiner; полный inventory
прежнего пилота не вызывается. Зависимость от внутреннего API helper является
техническим ограничением этого небольшого адаптера.

Локальный экземпляр политики: `frequency_map_policy: frequency-map-v1`,
`calculation_mode: fast_plot`, `spectrum_semantics: sorted_positions`,
`sweep_parameter: kappa_theta` на двух фиксированных углах,
`parameter_grid: [0,0.1,1,100,10000,RIGID]`, `K_plot: 6`, `K_guard: 7`,
`guard_root_role: completeness_only`, `neighbour_audit: monotonicity_on_accepted_cases`,
`local_repair_policy: triggered_only`, `strict_audit_default: false`.
Полная соседняя проверка не завершена из-за двух непринятых групп.

Подготовленный режим: сначала `beta=30°, kappa=1`, затем остальные группы
последовательно. Окна шириной до `4` по Omega, шаг не грубее `0.0625`, перекрытие
`0.125`, нижняя граница `1e-8`, аварийный потолок `240`. Оба detector работают
на каждом покрытом интервале; соседние результаты только смещают границы окон.
Подозрение вызывает один локальный повтор с более мелкой и сдвинутой сеткой.
Неустранённые кандидаты останавливают группу. Лимит `120 s` проверяется между
окнами; он не является измеренной стоимостью или гарантией длительности одного окна.

Остановка — после нахождения семи позиций с расстоянием guard от правого края
окна больше `0.02`. Дополнительное окно/хвост над найденным guard не запускается;
неизбежная часть последнего окна справа от guard фиксируется в diagnostics.
Это конечный detector-контроль, не теорема полноты. `consolidate` допускает
объединение повторных обнаружений только при перекрытии интервалов, согласованной
nullity и расстоянии ≤64 ULP; прочие близкие кандидаты требуют локального
разрешения. Кратность берётся из nullity, не из числа обнаружений. Если кратный
корень пересекает границу guard, группа останавливается без усечения кратности.
При исполнении потребовалась локальная сверка повторных обнаружений за пределами
64 ULP: расстояние лишь выделяет подозрение, а решение требует пересечения
исходных интервалов, монотонного изменения определителя с одной сменой знака на
девяти точках и отделённости второго сингулярного числа по действующему порогу
`1e-9`. Сохраняется лучший принятый determinant-кандидат, без усреднения частот.
Для одной неуточнённой sigma-оценки в rigid regression применён прежний refiner
от уже разрешённого determinant-корня после такой локальной проверки.
Это конечная диагностика простого корня, не доказательство полноты.

## Контрольные проверки и их статус

Фактически выполненные проверки и их пределы:

- [Целевые тесты](../../tests/test_inplane_rotational_spring_eb.py): шесть скалярных
  условий, пять прежних строк, работа и гессиан пружины, шарнир/RIGID,
  минор/ранг, EB state, масштабирование, обратный перевод реакций, валидация входа,
  отделение продольного блока при beta=0, сохранение неразрешённых близких кандидатов.
  **50 passed in 0.73 s** на конечной версии файлов этого продолжения.
- На каждом корне: полная матрица, nullity, нуль-вектор и шесть физических остатков;
  при шарнире дополнительно оба момента. Все 70 принятых позиций имеют nullity=1;
  максимум `sigma_min/sigma_max = 9.807e-15`, нормированной невязки нуль-вектора
  `6.929e-15`, поступательной совместности `1.478e-13`, всех физических строк
  `1.099e-10`. Для двух шарнирных групп максимум обоих моментов в единицах
  `D_ref/l` равен `4.037e-12`. Сохранены все шесть остатков, не только определитель.
- Независимый прямой RIGID reference: объединение продольных
  `omega_n=n*pi/(2*l)*sqrt(A/m)` и изгибных корней `cos(z)-1/cosh(z)=0`,
  `Omega=z^2/4`, для длины `2*l`, только в диапазоне guard. Максимальная
  относительная разность семи частот **3.607e-13**.
- RIGID regression при обоих углах: семь локальных окрестностей старой
  `assemble_clamped_coupled_matrix`, `mu=0`, `eps=h/(sqrt(12)*l)` и аргумент
  `Lambda=sqrt(Omega)`. Совместимость следует из раздела
  [«Представление EB при разных толщинах плеч»](rectangular_weakly_orthotropic_mu_beta_graphs_note.md).
  Это регрессия с общим detector, не полностью независимая верификация.
  При 0° максимум по семи корням **3.023e-13**; при 30° первые шесть совпали
  с максимумом **2.606e-13**, но локальный контроль седьмого не завершён (ниже).
- При beta=0 проверяются только продольные частоты, попавшие под guard данной
  группы. Их sorted index может меняться; пустой диапазон получает `NOT_IN_RANGE`.
  В пяти принятых прямых группах единственная продольная частота под guard
  равна `Omega=108.82796185405307` и совпала с reference; это текущая позиция 7.
  Спектр двух независимых консолей не используется как reference шарнира.
- Для `kappa=100` реальные относительные отличия первых шести частот от RIGID:
  при 0° `(3.65755e-3, 9.22e-16, 4.97138e-3, 7.82e-15, 4.87343e-3, 6.24e-13)`;
  при 30° `(1.73e-15, 6.83274e-3, 1.15192e-4, 4.78e-14, 2.93373e-3, 1.25e-13)`.
  Сравнение двух больших конечных жёсткостей **не завершено**: `10000` не принят.
  Неубывание проверено только на принятой последовательности `0,.1,1,100,RIGID`;
  минимальное относительное приращение `-8.458e-13`, в пределах `1e-9`.
  Равенство конечной пружины RIGID до машинного нуля не требовалось.
- Для представительной группы — отдельное локальное уточнение семи корней после
  положительного умножения строк на `(2,.5,3,1/3,5,.2)` и проверка физических остатков.
  Максимальная относительная разность частот **1.693e-13**, физическая невязка
  **9.094e-12**. Это отдельный контроль, не семь новых BASE-точек.

Для всех принятых BASE-групп неразрешённых кандидатов ниже guard не осталось;
минимальное расстояние guard до правой границы окна **0.25 по Omega**.
В завершённых группах зарегистрированы 50 локальных интервалов восстановления
и 39 сопоставлений повторных обнаружений. Это служебные проверки десяти групп,
не дополнительные физические точки и не доказательство полноты всей области.

## Неустранённые случаи

- `beta0_k10000`: BASE-группа отклонена. Локальный повтор около пятой позиции
  дал `Omega=74.63515469429801`, остаток пружинной строки **-1.01691e-9**
  при допуске `1e-9`. Эта диагностическая оценка не включена в BASE CSV.
- `beta30_k10000`: отказ на guard `Omega=109.81555610724848`, остаток
  пружинной строки **1.23405e-9**. Группа целиком не принята.
- `beta30_RIGID`: собственные BASE-невязки и guard прошли, но legacy detector
  вернул около guard оценки `109.8155561072413` и `109.81555614951644`.
  Оба кандидата удовлетворяют его порогу nullity, однако предусмотренное
  сопоставление их не разрешило. Они не объединены только по расстоянию;
  полная семикорневая regression не объявляется пройденной.

Первые два отказа показывают различие между малой матричной сингулярностью
и требуемой точностью восстановленного закона пружины при большой жёсткости.
Причина на уровне погрешностей переноса/восстановления ещё не установлена;
механические знаки, нормировка reference и допуски не менялись ради принятия.
Новый метод решения для закрытия этих случаев не разрабатывался.

Тесты строк/энергии/масштабирования и невязки являются проверками внутренней
согласованности. Одиночные характеристические уравнения дают независимый
прямой reference. Этот конечный набор не составляет полной независимой
верификации произвольного углового упругого соединения; RLB-спектры не затрагиваются.

## Среда, результаты и воспроизведение

Документированные команды проекта используют `python` из корня checkout.
В этом сеансе проверены `.venv`, `venv`, `env` (отсутствуют), доступные команды
`python`, `python3`, `py`, `uv`, `conda` и узкие стандартные каталоги установки.
`uv`/`conda` и соответствующая локальная среда не обнаружены; широкий поиск диска не выполнялся.

| Фактически выполненная проверка | Результат |
| --- | --- |
| `Get-Command python,python3,py,uv,conda -ErrorAction SilentlyContinue` | python/python3 — WindowsApps aliases; py — launcher; uv/conda не найдены |
| `py -0p` | `No installed Pythons found!` |
| `python -B -c "import sys; print(sys.executable); print(sys.version); import numpy, scipy; print(numpy.__version__, scipy.__version__); import pytest; print(pytest.__version__)"` | ОС отказала в доступе к alias; Python-код не исполнялся |
| `python3 --version` | ОС: `The file cannot be accessed by the system` |

Пути найденных команд: `C:/Users/Nikita/AppData/Local/Microsoft/WindowsApps/python.exe`,
аналогичный `python3.exe`; launcher:
`C:/Users/Nikita/AppData/Local/Programs/Python/Launcher/py.exe`.
Эта первоначальная проверка была неполной: она пропустила существующий Python
на диске D. После уточнения пользователя найден `D:/python/python.exe` (3.12.4,
без numpy/scipy/pytest в его путях) и рабочее окружение
**`D:/python/Pycharm/pythonProject/.venv/Scripts/python.exe`**.
Фактически импортированы Python **3.12.4**, numpy **2.1.3**, scipy **1.15.2**,
pytest **8.3.4**. Отсутствие рабочего запуска через PATH не означает отсутствия
Python на устройстве. Ничего не устанавливалось; PATH не менялся.

Фактически выполнены проверка импортов, целевые тесты, представительная группа,
последовательный пилот до отказа физического допуска и продолжение остальных
фиксированных случаев через `--case-id`. Команды из корня checkout:

```powershell
& 'D:\python\Pycharm\pythonProject\.venv\Scripts\python.exe' -B -m pytest -q tests/test_inplane_rotational_spring_eb.py
& 'D:\python\Pycharm\pythonProject\.venv\Scripts\python.exe' -B scripts/analysis/laminated_beams/pilot_inplane_rotational_spring_eb.py --benchmark-only
& 'D:\python\Pycharm\pythonProject\.venv\Scripts\python.exe' -B scripts/analysis/laminated_beams/pilot_inplane_rotational_spring_eb.py
& 'D:\python\Pycharm\pythonProject\.venv\Scripts\python.exe' -B scripts/analysis/laminated_beams/pilot_inplane_rotational_spring_eb.py --case-id beta0_RIGID
```

Аналогично по одному запущены `beta30_k0`, `beta30_k0.1`, `beta30_k100`,
`beta30_k10000`, `beta30_RIGID`; это прежние точки набора, не его расширение.
Представительная BASE-группа потребовала **21.96 s**; завершённые BASE-группы
после исправлений локальных контролей не пересчитывались.
Суммарное фактическое время десяти завершённых BASE-поисков — **235.03 s**,
сохранённых завершённых контролей — **10.60 s**. Полное время всех неудачных
попыток не было сохранено; его нельзя восстановить из этих сумм. Поле
`elapsed_seconds` manifest относится только к последнему запуску, это явно
оговорено в `timing_qualification`; совокупное время всех попыток — `null`.
Целевой каталог `results/laminated_beams/inplane_rotational_spring_eb_pilot/`
содержит [spectrum_roots.csv](../../results/laminated_beams/inplane_rotational_spring_eb_pilot/spectrum_roots.csv),
[diagnostics.json](../../results/laminated_beams/inplane_rotational_spring_eb_pilot/diagnostics.json),
[run_manifest.json](../../results/laminated_beams/inplane_rotational_spring_eb_pilot/run_manifest.json).
В них сохранены размерная omega, Omega, Lambda, обе жёсткости, отдельная метка
RIGID, семь позиций и кратности; параметры/допуски, хеши прочитанных зависимостей
кода, фактические версии среды и Git, время, новые/повторно использованные группы
и локальные восстановления. Контроли отделены от BASE.

`diagnostics.json` служит атомарной записью завершённых групп; CSV и manifest
восстанавливаются из него без повторного решения BASE. Несовпадение контракта
или версии кода/среды запрещает перезапись. Старые результаты не используются
для заполнения нового пилота. Частичный результат и причина остановки сохраняются.
В ходе локальных доработок runner проверялось неизменное содержимое физических
зависимостей; сохранённые BASE-строки перенесены без пересчёта с прежними хешами
в `source_code_sha256` и пояснением в `contract_migrations`.
Файлы результатов реально прочитаны, но игнорируются правилом `results/*`;
наличие этой tracked-заметки не означает, что CSV будут в другой копии checkout.

Первый запуск тестов дал 47 успешных проверок и один сбой reference: размерный
`expm` в тесте содержал шум в аналитически нулевых элементах при `atol=0`.
Reference заменён точным статическим интегрированием EB в безразмерных единицах
с ранее объявленным допуском `1e-12`; затем пройдены 48 тестов, после добавления
двух проверок локального сопоставления — 50. Физические формулы не менялись.
Проверки RLB с новой пружиной не проводились; вязкость не реализована;
общий PASS модели не присвоен. Продолжение на RLB автоматически не разрешено.
Место остановки — два отказа физических остатков при большой жёсткости и один
неразрешённый контроль legacy guard; блокер среды устранён.
