# Figure Index

Curated copies in this folder were copied from existing project outputs or
article-ready figures. No plots were regenerated.

## Figures

- `lambda_mu_beta15_r5mm.png`
  - Source: `paper_dorofeev_style/figures/lambda_mu_beta15_r5mm.png`.
  - Scope: `Lambda(mu)` for `beta = 15 deg`, `r = 5 mm`.
  - Use: first overview of the tracked frequency branches, FEM markers, and
    clamped-supported single-rod reference curves.
  - Status: a separate ready PNG graph for `Lambda(mu)` at `beta = 15 deg`,
    `r = 5 mm` exists and has been copied here.

- `target_descendant_beta15_r5_bending_desc_01_selected_shapes.png`
  - Source: `results/target_descendant_beta15_r5_bending_desc_01_selected_shapes.png`.
  - Branch: `bending_desc_01`.
  - Use: qualitative check of the suspected modal-character change.

- `target_descendant_beta15_r5_bending_desc_02_selected_shapes.png`
  - Source: `results/target_descendant_beta15_r5_bending_desc_02_selected_shapes.png`.
  - Branch: `bending_desc_02`.
  - Use: qualitative control case; expected to be useful as a non-veering
    comparison.

- `target_descendant_beta15_r5_bending_desc_04_selected_shapes.png`
  - Source: `results/target_descendant_beta15_r5_bending_desc_04_selected_shapes.png`.
  - Branch: `bending_desc_04`.
  - Use: qualitative check of the strongest current veering / modal-exchange
    candidate.

- `flat_mu_bending_energy_summary.png`
  - Source: `results/flat_mu_bending_energy_summary.png`.
  - Scope: existing bending-energy summary for flat-`mu` cases.
  - Use: qualitative support for localization / arm-energy imbalance,
    especially for `bending_desc_04`; see `data/dataset_index.md` for the
    exact table coverage.

## First Qualitative Use

The current figure set is enough for a first qualitative pass: compare the
`Lambda(mu)` overview with selected mode-shape panels, then cross-check
`bending_desc_04` against the existing arm-energy summary.

It is not enough for a strict veering claim without branch-pair gap and
modal-correlation data.
